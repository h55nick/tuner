.columns.nine.centered
  .columns.three.centered
    %h3
      Login
  =form_tag('/login') do
    =label_tag("username")
    =text_field_tag('username', nil,:autofocus => true)
    =label_tag('password')
    =password_field_tag('password')

    =submit_tag( 'Login' , :class=>"radius button centered")

